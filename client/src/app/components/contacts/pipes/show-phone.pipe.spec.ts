import { ShowPhonePipe } from './show-phone.pipe';

describe('ShowPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
